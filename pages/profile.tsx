import { NextPage } from 'next';
import { NavWrapper } from '../components/NavWrapper';

const Profile: NextPage = () => {
    return <NavWrapper>Todo: Profile page</NavWrapper>;
};

export default Profile;
